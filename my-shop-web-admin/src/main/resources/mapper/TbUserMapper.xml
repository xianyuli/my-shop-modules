<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xianyuli.my.shop.web.admin.dao.TbUserDao">
    <sql id="TbUserColumns">
        id,
        username,
        password,
        phone,
        email,
        created,
        updated
    </sql>
    
    <select id="selectAll" resultType="com.xianyuli.my.shop.domain.TbUser">
        SELECT
          <include refid="TbUserColumns"/>
        FROM
        tb_user
    </select>
    
    <insert id="insert" parameterType="com.xianyuli.my.shop.domain.TbUser">
        INSERT INTO `tb_user` (
        `username`, `password`, `phone`, `email`, `created`, `updated`
        )
        VALUES
            (#{username}, #{password}, #{phone}, #{email}, #{created}, #{updated} )
    </insert>

    <delete id="delete">
        DELETE FROM tb_user WHERE id=#{id}
    </delete>

    <select id="getById" resultType="com.xianyuli.my.shop.domain.TbUser">
        SELECT * FROM tb_user WHERE id=#{id}
    </select>

    <update id="update" parameterType="com.xianyuli.my.shop.domain.TbUser">
        UPDATE `myshop`.`tb_user`
            SET `username` = #{username},
            `password` =  #{password},
            `phone` =  #{phone},
            `email` =  #{email},
            `created` =  #{created},
            `updated` =  #{updated}
        WHERE
	         `id` = #{id};
    </update>

    <select id="selectByUsername" resultType="com.xianyuli.my.shop.domain.TbUser">
        SELECT * FROM `tb_user` where username LIKE CONCAT('%',#{username},'%')
    </select>
</mapper>